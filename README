html-templates is the testing ground

    /*
    // infinite sideways - doesn't work
    var bgWidth = 2120; // pixel height of background image
    $('body').width(bgWidth + $(window).width());
    $(window).scroll(function() {
      if ($(window).scrollLeft() >= ($('body').width() - $(window).width())) {
        $(window).scrollLeft(1);
      } else if ($(window).scrollLeft() == 0) {
        $(window).scrollLeft($('body').width() - $(window).width() -1);
      }
    });
*/
  /* more infinite sideways
  //$(window).resize(function() {
  //  $('body').width(bgWidth + $(window).width());
  //});
*/

<!-- example scrolling text -->
<!-- $.scrollTo( 1000, 800, {easing:'swing',axis:'x'} ); -->